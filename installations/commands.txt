
cd [file | dir]				#change directory
rm [file | dir]				#remove file or directory
cp [file | dir]				#copy file or directory
mv [file | dir] 			#move file or directory
hostnamectl set-hostname [hostname]	#change hostname
adduser [username]			#add a new user
adduser [username] sudo			#add the user to sudo group (allow root commands)
userdel [username]			#deleting user
chmod [u+r, g-w, o-x, a-r ... (
	u - user,
	g - group,
	o - others,
	a - all )
        (
	r - read,
	w - write,
	x - execute ) ] 
    [file name | dir name]		#change access of file or directory (change mode)
chown [user] [file | dir]		#change owner of file or directory

ufw default allow outgoing
ufw default deny incoming
ufw allow ssh
ufw allow [port number]
ufw enable
ufw disable
ufw status
ufw deny from [IP address] 

python3 -m venv [dir name] 		#create a new virtual environment
source [venv name]/bin/activate 	#activate the virtual environment
deactivate 				#deactivate the virtual environment

wget

###########
### git ###
###########

git reset --hard
git clean -fxd

#############
### mysql ###
#############

ALTER USER 'root'@'localhost' identified WITH mysql_native_password BY  'password';
flush privileges;

# updating data
UPDATE mytable SET column1 = value1, column2 = value2 WHERE key_value = some_value;

##############
### ubuntu ###
##############

# apache for python
apt-get install libapache2-mod-wsgi-py3

# mysql
sudo apt install mysql-server
sudo apt install mysql-client
sudo apt-get install python3-dev default-libmysqlclient-dev build-essential

###################
###package names###
###################

djangorestframework
djoser
django-cors-headers
django-debug-toolbar
